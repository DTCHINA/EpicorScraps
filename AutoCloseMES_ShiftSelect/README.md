Customization for the MES "Shift Select" dialog popup. This is a bit of a hack, but it gets the job done for what we needed. All it does is force a click on the 'OK' button as soon as the dialog pops up.
